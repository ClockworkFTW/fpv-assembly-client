import styled from "styled-components";

export const Wrapper = styled.div`
  border-radius: 4px;
  overflow: hidden;
`;

export const Image = styled.img`
  width: 100%;
  aspect-ratio: 3/2;
  border-radius: 4px;
  object-fit: cover;
`;
